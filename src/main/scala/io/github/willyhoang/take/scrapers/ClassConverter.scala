package io.github.willyhoang.take.scrapers

/**
 *
 */
object ClassConverter {

  /** We want to also normalize all of our fields. Levels should be consistent, genres should be
   * consistent as well. Times should also be consistent.
   */
//
//  def convertClass(input: GenericClass) : NormalizedClass = {
//    input match {
//      case BDCClass(time, style, level, instructor) =>
//        val times = time.split("-")
//        new NormalizedClass("", times(0), times(1), "BDC", instructor, style, level)
//      case _ => throw new IllegalArgumentException("Unknown input class type")
//    }
//  }

}
